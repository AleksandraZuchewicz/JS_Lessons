let dog = 4;
let cat = 5;

// sample of if statement
if (dog>6) {
  console.log("dog is bigger than 6");
}

// sample of switch statement
switch (dog) {
  case 12:

    break;
  case 6:

    break;
  default:

}

// sample of cammel notation
let catAndDog = dog > 6 && cat < 6;
if (catAndDog == true) {
  console.log("OK");
}
